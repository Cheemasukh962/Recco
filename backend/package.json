{
  "name": "recco-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Recco Person B backend: Convex functions for people, reactive app state, face matching, voice command interpretation, and opener drafting.",
  "scripts": {
    "dev": "convex dev",
    "codegen": "convex codegen",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "smoke": "tsx scripts/smoke.ts",
    "enroll": "tsx scripts/enroll.ts",
    "verify": "npm run typecheck && npm run test && npm run smoke"
  },
  "dependencies": {
    "convex": "^1.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
